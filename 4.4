#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main ()
{
	int mas[20], i, x, n = 20 , k = 0;
	srand(time(0));
	
	for(i = 0; i <= n; i++) 
	{
	mas[i] = rand() % 100;
	
	printf("\nmas[%d] = %d", i, mas[i]);
	
	}	
	
	for (i = 0; i < 20; i++)
	{
	if(mas[i] > 70)
 k++;
	}	
	printf("\nBilshe chem 70 %d chisel", k);
	
	
	return 0;	
}
